{
  "name": "firebase-mcp",
  "version": "1.0.0",
  "description": "Firebase Hosting MCP server for GTM Wizards — deploy HTML to hub.baselinepayments.com or preview channels. stdio (local) and HTTP/SSE (Cloud Run).",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "npx tsx src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "express": "^4.18.2",
    "google-auth-library": "^9.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.3.0"
  }
}
