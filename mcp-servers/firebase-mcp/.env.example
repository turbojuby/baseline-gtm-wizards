# No API tokens needed — Firebase uses Google Application Default Credentials
# Run: gcloud auth application-default login
# On Cloud Run, the service account is used automatically.

# Transport mode (set automatically by index.ts)
# MODE=stdio   — for local Claude Code usage (default)
# MODE=http    — for GCP Cloud Run deployment
MODE=stdio
